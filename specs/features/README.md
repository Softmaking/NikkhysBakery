# Feature Specifications

Store approved feature artifacts under `specs/features/<feature-name>/`.
Use the templates in `.sdd/templates/` and keep the artifacts synchronized with
the root business documentation, cross-application contract and affected repository docs.
